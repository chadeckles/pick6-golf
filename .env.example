# ─── Required ────────────────────────────────────────────────
# Used to sign/verify the session JWT. Must be long and random.
# Generate with: openssl rand -base64 48
JWT_SECRET="replace-me-with-a-long-random-string"

# ─── Recommended for production ──────────────────────────────
# Canonical origin the app is served from. Used for CSRF origin
# checks on mutating API routes and for building email links.
APP_URL="https://yourdomain.com"

# Where SQLite lives. On Railway, point this at your persistent
# volume (e.g. /data/masters-pick6.db).
DATABASE_PATH="./masters-pick6.db"

# ─── Email (optional; required to actually send reset links) ─
# Leave unset in dev — reset links will log to the server console.
# EMAIL_PROVIDER="resend"
# EMAIL_FROM="Pick 6 Golf <noreply@yourdomain.com>"
# RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxx"

# ─── Nightly backup ──────────────────────────────────────────
# Shared secret used by the GitHub Actions `backup.yml` workflow to
# authenticate to /api/admin/backup. Must be at least 24 chars.
# Generate with: openssl rand -base64 32
# Set the SAME value in GitHub repo secrets as BACKUP_API_KEY (plus APP_URL).
# BACKUP_API_KEY="replace-me-long-random-string"
